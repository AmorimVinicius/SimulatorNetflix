import styled from 'styled-components';

export const Pagina404Base = styled.body`
  background-color: #141414;
  min-height: calc(100vh - var(--bodyPaddingTop));
  display: flex;
  flex-direction: column;

  label {
    font-size: 2em;
    color: #ffffff;
    top: 50%;
    margin-top: 50px;
    text-align: center;
  }
`;
